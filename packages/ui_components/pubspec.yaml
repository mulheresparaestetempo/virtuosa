name: ui_components
description: Reusable UI components for Abba Virtuosa
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.13.0'

dependencies:
  flutter:
    sdk: flutter
  design_system:
    path: ../design_system

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
